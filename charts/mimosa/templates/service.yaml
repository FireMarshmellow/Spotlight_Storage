apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimosa.fullname" . }}
  labels:
    {{- include "mimosa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mimosa.selectorLabels" . | nindent 4 }}
